numpy>=1.24.0
pandas>=2.0.0
pytz>=2023.3
requests>=2.31.0
scipy>=1.10.0
streamlit>=1.28.0
yfinance>=0.2.28

# --- Krupp Capital dark-institutional GEX charts (2026-07 chart feature) ---
matplotlib>=3.8.0
flask>=3.0.0
Pillow>=9.1.0     # required for the Telegram sendPhoto 1280-px safety net (LANCZOS resize)

# --- Quant Metrics / Strategy + MC / Greeks tabs (2026-07 dashboard expansion) ---
plotly>=5.18.0    # tab modules `import plotly.graph_objects as go` directly (volsurface, candles, payoff, MC)
